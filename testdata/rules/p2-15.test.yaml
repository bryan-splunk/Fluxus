cases:
  - name: "fires when iis receiver present"
    config: |
      receivers:
        iis:
          collection_interval: 10s
    should_fire: true

  - name: "silent when iis absent"
    config: |
      receivers:
        otlp: {}
    should_fire: false
